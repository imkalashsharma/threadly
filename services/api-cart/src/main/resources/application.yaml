spring:
  application:
    name: api-cart

  datasource:
    url: jdbc:postgresql://localhost:5433/threadly_cart
    username: threadly
    password: threadly

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true