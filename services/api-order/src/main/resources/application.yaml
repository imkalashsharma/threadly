spring:
  application:
    name: api-order

  datasource:
    url: jdbc:postgresql://localhost:5433/threadly_order
    username: threadly
    password: threadly

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true